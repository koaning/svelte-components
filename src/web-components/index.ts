import Terminal from '../components/Terminal.svelte';

// All components are automatically registered as custom elements
// due to the customElement option in each Svelte component
export { Terminal };